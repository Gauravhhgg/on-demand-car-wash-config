server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/washerdb
    username: root
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
